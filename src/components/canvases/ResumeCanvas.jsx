import { StubCanvas } from "../ModuleCanvas";

export default function ResumeCanvas() {
  return (
    <StubCanvas
      icon="📋"
      title="Resume · Service Record"
      tagline="Your living service record. Tell Peraasan what you did. When a job posting lands, get a tailored resume drawn from your real entries — not a generic doc rewritten in panic."
      comingSoon="Journal as a real list view (not just a chat card) with category filter pills, search, and one-click edit. Tailor flow as a focused side-by-side workflow — your record on the left, the JD on the right, the tailored output in the middle."
      paneHint="Log a work entry, show your record, or tailor a resume to a job from the left pane. Use Peraasan chat for conversational capture ('Just shipped the multi-region failover...')."
    />
  );
}
