import { StubCanvas } from "../ModuleCanvas";

export default function PathsCanvas() {
  return (
    <StubCanvas
      icon="🎯"
      title="Paths"
      tagline="Goal in. Live learning path out. Auto-adjusts on session, content, staleness, or assignment events. Multi-goal weighted scheduling against your real calendar — every learning block is a real Calendar event with a 5-minute reminder."
      comingSoon="Goal cards open into per-step path detail with status badges, mark-done / skip / reorder actions inline. Calendar view of upcoming learning blocks. Path-adjustment history surfaced as a timeline."
      paneHint="Show your goals, add a new one, or trigger the Path Engine from the left pane. Use Peraasan chat for the conversational add-goal flow and 'what's my next step?' recommendations."
    />
  );
}
